server:
  port: 8080

spring:
  application:
    name: flowpay-backend
  jackson:
    serialization:
      write-dates-as-timestamps: false
#  datasource:
#    url:
#      jdbc:postgresql://localhost:5432/flowpay username:
#        postgres password: postgres
#    jpa:
#      hibernate:
#        ddl-auto: update
#        show-sql: true
#        properties:
#          hibernate:
#            format_sql: true
  datasource:
    url: jdbc:h2:mem:flowpay
    driverClassName: org.h2.Driver
    username: sa
    password:
  docker:
    compose:
      enabled: false

  h2:
    console:
      enabled: true
      path: /h2-console

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.hibernate.SQL: DEBUG
